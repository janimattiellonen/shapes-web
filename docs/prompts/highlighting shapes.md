#Highlighting shapes

Optionally, a bounding border should be rendered around the detected shape.

In the frontend, there should be a checkbox with the label "Draw bounding border" checkbox. If checked, then show the bounding border.